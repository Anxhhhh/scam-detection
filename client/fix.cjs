const fs = require('fs'); const lines = fs.readFileSync('src/App.tsx', 'utf8').split('\n'); lines[933] = "          { step: '2', title: 'Revoke all existing permissions', detail: 'Go to Settings -> Apps -> [App name] -> Permissions. Revoke access to contacts, SMS, storage, camera, and microphone.' },"; lines[934] = "          { step: '3', title: 'Uninstall the app immediately', detail: 'Remove the app from your device. If you cannot uninstall it, restart your phone in Safe Mode (hold power button -> Safe Mode) and uninstall from there.' },"; fs.writeFileSync('src/App.tsx', lines.join('\n'), 'utf8'); console.log('Fixed');
